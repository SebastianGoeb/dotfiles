[color]
  ui = auto
[core]
  excludesfile = ~/.gitignore
  safecrlf = warn
  autocrlf = input
[pull]
  ff = only
[push]
  default = simple
[user]
  name = Sebastian Göb
  email = sebastian.goeb@main-method.com
[filter "lfs"]
  process = git-lfs filter-process
  required = true
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f

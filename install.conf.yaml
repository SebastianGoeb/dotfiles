- defaults:
    link:
        create: true
        relink: true
- clean: ['~']
- link:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.shell:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:
# - shell:
    # any git commands have to run after linking ~/.gitconfig

- defaults:
    link:
        create: true
        relink: true
- clean: ['~']
- link:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.zshrc:
# - shell:
    # any git commands have to run after linking ~/.gitconfig
